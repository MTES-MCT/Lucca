######################################################
## Prod env
## Override security with cas authentication
######################################################
security:
    firewalls:

        # Main firewall
        main:
            # configure the maximum login attempts (per minute)
            login_throttling:
                max_attempts: 3
                interval: '15 minutes'
