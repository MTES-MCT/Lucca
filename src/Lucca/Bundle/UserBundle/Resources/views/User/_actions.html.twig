<div class="btn-group">
    <a href="{{ path('lucca_user_show', { 'id': id }) }}" class="btn btn-primary btn-sm"><i class="fa fa-eye"></i></a>
    <a href="{{ path('lucca_user_edit', { 'id': id }) }}" class="btn btn-warning btn-sm"><i class="fas fa-pencil-alt"></i></a>
    <a href="{{ path('lucca_user_enable', { 'id': id }) }}" class="btn btn-{{ enabled ? 'info' : 'danger' }} btn-sm">
        <i class="fa fa-toggle-{{ enabled ? 'on' : 'off' }}"></i>
    </a>
</div>
