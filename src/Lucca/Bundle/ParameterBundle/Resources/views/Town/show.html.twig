{#-- Extends view --#}
{% extends '@LuccaThemeStreamDashboardUiKit/SubLayout/show.html.twig' %}

{#-- Translation domain --#}
{% trans_default_domain 'LuccaParameterBundle' %}

{#-- Parameters --#}
{% set entity_trans='entity.town'|trans %}
{% block menuActive_parameter %}active{% endblock menuActive_parameter %}


{% block lucca_panels %}
    {#-- Panel - main --#}
    <div class="col-lg-6 col-md-8 col-sm-12 col-xs-12">

        <div class="card card-default">
            <div class="card-header">
                {{ 'title.show'|trans({}, 'LuccaThemeStreamDashboardUiKit') }} - <b>{{ town.name }}</b>
            </div>

            <div class="card-wrapper">
                <div class="card-body">

                    <div class="col-12">
                        <table class="table table-striped table-hover">
                            <tbody>
                            <tr>
                                <th><i class="fa fa-road"></i> {{ 'label.code'|trans }}</th>
                                <td>{{ town.code }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'label.name'|trans }}</th>
                                <td>{{ town.name }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'label.enabled'|trans }}</th>
                                <td>{{ town.enabled|boolean }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'label.zipcode'|trans }}</th>
                                <td>{{ town.zipcode }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'label.office'|trans }}</th>
                                <td>{{ town.office }}</td>
                            </tr>
                            <tr>
                                <th>{{ 'label.intercommunal'|trans }}</th>
                                <td>{% if town.intercommunal %}{{ town.intercommunal.name }}{% endif %}</td>
                            </tr>
                            </tbody>
                        </table>
                    </div>

                    <div class="col-12 mt-4">
                        <a class="btn btn-secondary btn-lg" href="{{ path('lucca_town_index') }}"
                           title="{{ 'link.town.backList'|trans }}"><i class="fa fa-reply"></i></a>

                        <a class="btn btn-warning btn-lg"
                           href="{{ path('lucca_town_edit', { 'id': town.id }) }}"
                           title="{{ 'link.town.edit'|trans }}"><i class="fas fa-pencil-alt"></i></a>

                        <a class="btn btn-danger btn-lg pull-right" data-toggle="modal"
                           data-target=".delWin" title="{{ 'link.town.delete'|trans }}">
                            <i class="fas fa-times"></i>
                        </a>
                    </div>

                </div>
            </div>
        </div>
    </div>
    {#-- End Panel - main --#}
{% endblock lucca_panels %}


{% block lucca_css %}
    {{ parent() }}
{% endblock lucca_css %}


{% block lucca_js %}
    {% include '@LuccaThemeStreamDashboardUiKit/Modal/delete.html.twig' with {'id': town.name, 'name': town.name, 'delForm': delete_form} %}
    {{ parent() }}
{% endblock lucca_js %}
