{#-- Extends view --#}
{% extends '@LuccaThemeStreamDashboardUiKit/SubLayout/index.html.twig' %}

{#-- Translation domain --#}
{% trans_default_domain 'LuccaParameterBundle' %}

{#-- Parameters --#}
{% set entity_trans='entity.intercommunal'|trans %}
{% block menuActive_parameter %}active{% endblock menuActive_parameter %}


{% block lucca_list %}

    <table class="table table-striped table-hover dataTables responsive">
        <thead>
        <tr>
            <th><i class="fa fa-map"></i> {{ 'label.name'|trans }}</th>
            <th>{{ 'label.code'|trans }}</th>
            <th>{{ 'label.office'|trans }}</th>
            <th><i class="fa fa-cogs"></i> {{ 'label.actions'|trans }}</th>
        </tr>
        </thead>

        <tbody>
        {% for intercommunal in intercommunals %}
            <tr>
                <td>{{ intercommunal.name }}</td>
                <td>{{ intercommunal.code }}</td>
                <td>{% if intercommunal.office %}{{ intercommunal.office.name }}{% endif %}</td>
                <td>
                    {% if intercommunal.enabled %}
                        <a href="{{ path('lucca_intercommunal_enable', { 'id': intercommunal.id }) }}" class="btn btn-info btn-sm"
                           title="{{ 'link.intercommunal.disable'|trans }}">
                            <i class="fa fa-toggle-on" aria-hidden="true"></i>
                        </a>
                    {% else %}
                        <a href="{{ path('lucca_intercommunal_enable', { 'id': intercommunal.id }) }}"
                           class="btn btn-danger btn-sm" title="{{ 'link.intercommunal.enable'|trans }}">
                            <i class="fa fa-toggle-off" aria-hidden="true"></i>
                        </a>
                    {% endif %}

                    <a href="{{ path('lucca_intercommunal_show', { 'id': intercommunal.id }) }}"
                       class="btn btn-primary btn-sm"
                       title="{{ 'link.intercommunal.show'|trans }}"> <i class="fa fa-eye"></i> </a>
                    <a href="{{ path('lucca_intercommunal_edit', { 'id': intercommunal.id }) }}"
                       class="btn btn-warning btn-sm"
                       title="{{ 'link.intercommunal.edit'|trans }}"> <i class="fas fa-pencil-alt"></i> </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-secondary btn-lg" href="{{ path('lucca_core_parameter') }}"
       title="{{ 'link.parameter'|trans }}"><i class="fa fa-reply"></i></a>

    <a class="btn btn-primary btn-lg" href="{{ path('lucca_intercommunal_new') }}"
       title="{{ 'link.intercommunal.new'|trans }}"><i class="fa fa-plus"></i></a>
{% endblock lucca_list %}

